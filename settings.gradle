pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // flatDir { // If you have local AARs/JARs like OpenCV
        //     dirs 'libs'
        //     dirs 'app/libs'
        // }
    }
}
rootProject.name = "ImageProcessingApp"
include ':app'
// To include OpenCV as a module (if you have it locally)
// include ':opencv'
// project(':opencv').projectDir = new File(settingsDir, 'sdk/native/jni') // Adjust path to your OpenCV module
